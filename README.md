# ricky-g.com

Personal landing page for **Ricky Gummadi**
Live site: https://ricky-g.com

## Stack
Plain HTML + CSS. No build tooling, no frameworks, no dependencies.

- `index.html` — landing page
- `styles.css` — styles (light + dark mode)
- `CNAME` — custom domain (`ricky-g.com`)
- `.nojekyll` — serve files as-is
